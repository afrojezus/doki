module.exports = {
    serverRuntimeConfig: {
        PROJECT_ROOT: __dirname
    },
    distDir: 'dist',
    typescript: {
        // not a long term solution
        ignoreBuildErrors: true,
    }
}
